import styled from 'styled-components';

export const Address = styled.div`
	position: absolute;
	top: 1rem;
	right: -10%;
	transform: translateX(-50%);
	width: 100%;
	max-width: 400px;
	z-index: 10;
`;
